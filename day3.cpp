#include <iostream>
using namespace std;

int main() {
  /* //input
    int num1, num2;
    cout << "Enter two numbers: ";
    cin >> num1 >> num2;
    //output
    cout << "Sum: " << num1 + num2 << endl;
    */
   //arithmetic operators
   /* int a=10, b=3;
    cout<<"a + b = "<<a+b<<endl;//13
    cout<<"a - b = "<<a-b<<endl;//7
    cout<<"a * b = "<<a*b<<endl;//30
    cout<<"a / b = "<<a/b<<endl;//3
     cout<<"a / b = "<<a/(float)b<<endl;//3
    cout<<"a % b = "<<a%b<<endl;//1
    */
   //relational operators
   /*  int x=10, y=20;
    cout<<"x == y: "<<(x==y)<<endl;// x == y: 0
    cout<<"x != y: "<<(x!=y)<<endl;// x != y: 1
    cout<<"x > y: "<<(x>y)<<endl;// x > y: 0
    cout<<"x < y: "<<(x<y)<<endl;// x < y: 1
    cout<<"x >= y: "<<(x>=y)<<endl;// x >= y: 0
    cout<<"x <= y: "<<(x<=y)<<endl;// x <= y: 1
    */
   /*
    //logical operators-> used to combine multiple conditions
    // &&-> logical AND operator, ||-> logical OR operator, !-> logical NOT operator
     bool p=true, q=false;
    cout<<"p && q: "<<(p&&q)<<endl;// p && q: 0
    cout<<"p || q: "<<(p||q)<<endl;// p || q: 1
    cout<<"!p: "<<(!p)<<endl;// !p: 0
    cout<<"!q: "<<(!q)<<endl;// !q: 1
    */
   //unary operators-> used to perform operations on a single operand
// ++-> increment operator, ---> decrement operator, - -> unary minus operator
 //code example
   /* int i=5;
    cout<<"i: "<<i<<endl;// i: 5
    cout<<"++i: "<<++i<<endl;// ++i: 6
    cout<<"i++: "<<i++<<endl;// i++: 6
    cout<<"i: "<<i<<endl;// i: 7
    cout<<"--i: "<<--i<<endl;// --i: 6
    cout<<"i--: "<<i--<<endl;// i--: 6
    cout<<"i: "<<i<<endl;// i: 5  
    //prefix increment operator(++i) increments the value of i before using it in the expression, while postfix increment operator(i++) uses the current value of i in the expression and then increments it.
    //prefix decrement operator(--i) decrements the value of i before using it in the expression, while postfix decrement operator(i--) uses the current value of i in the expression and then decrements it.
    //postfix increment operator(i++) is generally more efficient than prefix increment operator(++i) when used in a loop or when the value of i is not needed immediately after the increment.
    //postfix decrement operator(i--) is generally more efficient than prefix decrement operator(--i) when used in a loop or when the value of i is not needed immediately after the decrement.
    */ 
return 0;
}